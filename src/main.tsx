import React from "react";
import ReactDOM from "react-dom/client";
import { LeaveScreen } from "./LeaveScreen/LeaveScreen.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LeaveScreen />
  </React.StrictMode>
);
